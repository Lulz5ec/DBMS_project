var express = require("express");
var router = express.Router({ mergeParams: true });
var Medicine = require("../models/medicine");
var Order = require("../models/order");
// const { delete } = require("./cart");



module.exports = router;